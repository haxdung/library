$("#books").html('<%= j render partial: 'books/book_detail', collection: @books, as: :book %>')
$("#paginate").html('<%= j paginate(@books, remote: true, theme: 'twitter-bootstrap-3') %>')
$("#request-section")[0].scrollIntoView();
